import React from "react";
import { AppBar, Toolbar } from "@material-ui/core";

const HeaderComponent = () => {
  return (
    <>
      <AppBar>
        <Toolbar>Employee CRUD Application</Toolbar>
      </AppBar>
    </>
  );
};

export default HeaderComponent;
